# Dexter Logger Module
# This module handles SQLite logging for exchanges
# Must not import Telegram modules, Gemini SDK internals, or retriever internals
# Must never raise exceptions that can crash the bot
# Must never block student responses